version: '3.8'

services:
  live-ai:
    image: mosqueiro/live-ai
    container_name: live-ai
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - STREAM_AUTO_START=true
    restart: unless-stopped
    
volumes:
  data:
    driver: local